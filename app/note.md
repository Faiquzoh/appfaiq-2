0. apa itu dan ciri routing dinamis dan statis?

1. pembuatan folder routing
2. pembuatan page.tsx utk halaman aplikasi
3. pembuatan komponen fungsi di react di next js
4. function parameter untuk routing dinamis, termasuk di dalamnya params, folder dengan kurung kotak, misalnya [nomorSurat] itu sebagai placeholder routing dinamis nya, params misalnya yang {params}:{params:Promise<TipeDataContoh>}
5. cara kerja penentuan tipe data untuk ketika membuat function parameter di typescript
- annotitation = inline
- declaration = internal (yang posisinya di atas), misalnya type = TipeDataContoh {
    nomorSurat : string
}

6. pembuatan function asychronous itu untuk apa?, untuk perintah yang membutuhkan data dari luar aplikasi, misalnya koneksi api, database online, dst 
7. apa itu JSON? response dari pemanggilan api yang berupa object, dan ada yang berupa array, dan untuk melihat jsonformatter jsonbeautifier, cara ambil datanya fgi,ana?, dan menentukan terurn dari function asynchronous seperti apa, 
8. pemanggilan data matang hasil fetching API yang di html nya yang kita tulis di return halaman komponen cara nya seperti apa
    misal <h1> Suhu : {cuaca.temperature}</h1>, dan seterusnya